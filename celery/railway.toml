[build]
builder = "RAILPACK"

[deploy]
startCommand = "celery -A sabrlingua worker --loglevel=info --pool=solo"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PYTHONUNBUFFERED = "1"

[phases.setup]
nixPkgs = ["ffmpeg"]