{
  "build": {
    "secrets": ["OPENAI_API_KEY"],
    "packages": {
      "python": true
    },
    "cache": {
      "pip": true
    },
    "commands": ["pip install --upgrade pip", "pip install -r requirements.txt"]
  },
  "start": "gunicorn sabrlingua.wsgi --log-file - --timeout 300 --workers 4"
}
